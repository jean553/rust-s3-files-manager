[package]
name = "rust-s3-files-manager"
version = "0.1.0"
authors = ["Jean.LELIEVRE@supinfo.com"]

[dependencies]
rocket = "0.2.0"
rocket_codegen = "0.2.0"
rusoto = {version = "0.23", features = ["s3"]}
hyper = "0.10.4"

[dev-dependencies]
rocket = {version = "0.2.0", features = ["testing"]}
